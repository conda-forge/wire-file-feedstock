# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json
schema_version: 1

context:
  version: "0.6.1"

package:
  name: wire-file
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/w/wire-file/wire_file-${{ version }}.tar.gz
    sha256: b18b5c58f92c6285a6b17b75707e254df389e5cf5f94b681dccbb4eeb819a349

build:
  number: 0
  noarch: python
  script:
    - ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation --disable-pip-version-check

requirements:
  host:
    - pip
    - python ${{ python_min }}.*
    - uv-build
  run:
    - python >=${{ python_min }}
    - wiredb >=0.5.0,<0.6.0

tests:
  - python:
      imports: wire_file
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/davidbrochart/wiredb
  summary: Wire for storing to a file.

extra:
  recipe-maintainers:
    - davidbrochart
